#include "main.h"

/**
 * mul - Function that multiples two integers.
 * @a: The variable integer a
 * @b: The variable integer b
 * Return: always 0 (success)
 */
int mul(int a, int b)
{
	int mul;

	mul = (a * b);

	return (mul);
}
